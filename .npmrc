# Scoped packages default to restricted on npm, and a restricted publish under a
# free org fails. Every package also declares `publishConfig.access`, so this is
# the repository-wide floor rather than the only statement of it.
access=public
